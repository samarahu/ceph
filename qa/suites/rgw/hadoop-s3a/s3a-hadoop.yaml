tasks:
- install:
    # extra packages added for the rgw-d4ntests task
    extra_system_packages:
      deb: ['redis']
      rpm: ['redis']
- ceph:
- redis:
    client.0:
- ssh-keys:
- dnsmasq:
    client.0: [s3.]
- rgw:
    client.0:
      dns-name: s3.
- s3a-hadoop:
    role: client.0
